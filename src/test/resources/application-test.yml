spring:
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

cloud:
  aws:
    credentials:
      accessKey: dummyAccessKey
      secretKey: dummySecretKey
    s3:
      bucket: dummyBucket

jwt:
  secret: dummyJwtSecret
